====================
<% if @user.name.present? %>
  <%= @user.name %> 様
<% else %>
  ユーザー 様
<% end %>
====================

いつもご利用ありがとうございます。
<%= @item.genre.name %>：<%= @item.name %>の期限切れ3日前（期限: <%= @item.exp %>）を
お知らせいたします。

マイページ : <%= @url %>