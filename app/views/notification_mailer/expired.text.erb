====================
<% if @user.name.present? %>
  <%= @user.name %> 様
<% else %>
  ユーザー 様
<% end %>
====================

いつもご利用ありがとうございます。
<%= @item.genre.name %>：<%= @item.name %>の期限になりましたので,
お知らせいたします。

マイページ : <%= @url %>